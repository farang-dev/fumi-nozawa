{
  "name": "fumi-nozawa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "deploy": "npm run build && npx gh-pages -d out",
    "export": "next export",
    "start": "next start",
    "lint": "next lint"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  },
  "dependencies": {
    "next": "^13.3.0"
  }
}
